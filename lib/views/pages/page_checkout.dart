import 'package:flutter/material.dart';

class PageCheckout extends StatelessWidget {
  const PageCheckout({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}

class TitleArticlePageCheckout extends StatelessWidget {
  const TitleArticlePageCheckout({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}